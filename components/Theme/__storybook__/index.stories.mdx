import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { THEMES } from "../constants";
import Theme from "../index";
import { ThemeVariants } from "./ThemeVariants.js";

import './index.scss';

<Meta
  title="Theme"
  component={Theme}
  argTypes={{
    themeName: {
      options: Object.values(THEMES),
      control: { type: "select" },
    },
  }}
/>

export const Template = ({
  themeName=''
}) => (
  <Theme
    themeName={themeName}
  >
    <div className='storybook-theme-container'>
      <h2>{themeName}</h2>
      <div className='storybook-theme-fields'>
        <div className='storybook-theme-field primary'>primary</div>
        <div className='storybook-theme-field primary-hover'>primary-hover</div>
        <div className='storybook-theme-field secondary'>secondary</div>
        <div className='storybook-theme-field secondary-hover'>secondary-hover</div>
      </div> 
    </div>
  </Theme>
);

# Theme

This is a warpper component that help us to support themes.

## Default theme

<Canvas>
  <Story name="default_props">{Template.bind({})}</Story>
</Canvas>

<Canvas>
  <Story name="all themes">
    <ThemeVariants>
      {(themeName) => Template({themeName})}
    </ThemeVariants> 
  </Story>
</Canvas>